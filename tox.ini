[tox]
minversion = 3.24.0
envlist = py36, py310, mypy
isolated_build = true

[gh-actions]
python =
	3.6: py36, mypy,
	3.10: py310

[testenv]
setenv =
	PYTHONPATH = {toxinidir}
deps =
	-r{toxinidir}/requirements_dev.txt
commands =
	mypy src
	pytest --basetemp={envtmpdir}